name: docker-build

on:
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'Dockerfile.server'
      - 'Dockerfile.web'
      - 'package*.json'
      - 'turbo.json'
      - 'tsconfig.base.json'
      - '.github/workflows/docker-build.yml'
  workflow_dispatch:

env:
  # Default registry is aliceastor (the repo owner that can actually push).
  # To switch to meaadore1221-afk, set ACTIONS_GHCR_OWNER=meaadore1221-afk below
  # AND provide a meaadore1221-afk PAT as the GHCR_PUSH_TOKEN secret
  # (the aliceastor GITHUB_TOKEN cannot create packages under another user).
  GHCR_OWNER: aliceastor
  IMAGE_PREFIX: megacorps

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: server
            dockerfile: Dockerfile.server
            context: .
            platforms: linux/amd64
          - name: web
            dockerfile: Dockerfile.web
            context: .
            platforms: linux/amd64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.GHCR_OWNER }}/${{ env.IMAGE_PREFIX }}-${{ matrix.target.name }}
          tags: |
            type=ref,event=branch
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.target.context }}
          file: ${{ matrix.target.dockerfile }}
          platforms: ${{ matrix.target.platforms }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
